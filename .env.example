# PostgreSQL connection settings
DATABASE_URL=postgres://postgres:postgres@localhost:5432/a_ite193
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=a_ite193

# optional database SSL mode
# PGSSLMODE=require

# session secret for express-session
SESSION_SECRET=replace-with-secure-secret
